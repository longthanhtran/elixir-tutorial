# Types of truth, name also atom true === :true
true
false
nil

# Operators
a === b
a !== b
a == b
a != b
a > b
a >= b
a < b
a <= b

# Strict boolean operators
a or b   # true if a is true, otherwise b
a and b  # false if a is false, otherwise b
not also # false if a is true, true otherwise

# Relax boolean operators
a || b # a if a is truthy, otherwise b
a && b # b if a is truthy, otherwise a
!a     # false if a is truthy, otherwise true

# Athrimetic operators
+ - * / div rem

# Join operators
bin1 <> bin2
list1 ++ list2
list1 -- list2

# in operator
a in enum
